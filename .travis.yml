language: node_js
sudo: required
services:
 - docker
 
before_install:
 - docker build -t scrouncha/docker-react -f Dockerfile.dev .
 
script:
 - docker run scrouncha/docker-react npm run test -- --coverage